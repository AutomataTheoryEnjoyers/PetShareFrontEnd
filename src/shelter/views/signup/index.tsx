import styled from "styled-components";
export const SignUp = () => {
  return <Container>shelter flow</Container>;
};

const Container = styled.div`
  background-color: ${(props) =>
    props.theme.colors.background
  };
`
